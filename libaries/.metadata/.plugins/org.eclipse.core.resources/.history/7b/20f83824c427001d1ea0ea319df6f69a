#include "msp.h"
#include <stdint.h>
#include <stdio.h>

typedef enum s_GPIO_IO
{
    input,
    output,
}s_GPIO_IO;

typedef enum s_GPIO_portNumber
{
    port1,
    port2,
    port3,
    port4,
    port5,
    port6,
    port7,
    port8,
    port9,
    port10,
};

typedef struct s_GVSU_GPIO
{

}s_GVSU_GPIO;
